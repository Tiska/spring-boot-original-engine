# Ces parametres sont accessible via le generatorRuntime
# en ajoutant c-generator.demo.persistence. devant le nom de 
# la propriete.
# Ce fichier sera ecrase lors de la prochaine generation
			
# Parametres de connexion a la base			
db.driver=com.mysql.jdbc.GoogleDriver
db.url=jdbc:google:mysql://model-folio-87421:spring-boot-demo-bdd?user=root
db.user=root
db.password=
db.syntax=mysql
			
# Parametres du pool.
db.mincount=0
db.maxcount=50
db.maxgets=1000
db.maxwait=30
db.maxwait=15
db.maxidle=300

# Parametres du HashProvider.
# indique si on est en mode test ou non.
hashProvider.test=false
# Permet d'indiquer une classe alternative pour la gestion des hash
# hashProvider.class=

# Parametres du cache
cache.maxidle=240
cache.expiration=1440

#########################
# Les implementations ...
#########################

# implementations pour demo.persistence.beans.dao.IClientDao
impl.demo.persistence.beans.dao.IClientDao=demo.persistence.beans.dao.impl.cg.ClientDaoCgImpl
